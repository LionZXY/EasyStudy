<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.lionzxy.easystudy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AnswersListActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".AnswerActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".QuestionsActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".LoginActivity" android:theme="@style/LoginStyle"/>
        <activity android:name=".ThemeListActivity" android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>