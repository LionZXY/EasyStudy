<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.lionzxy.easystudy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.bugsnag.android.API_KEY"
            android:value="52c5d2897e826c4b3ade2a56328dd74c"/>
        <activity android:name=".activitys.AnswersListActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".activitys.AnswerActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".activitys.QuestionsActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".activitys.LoginActivity" android:theme="@style/LoginStyle"/>
        <activity android:name=".activitys.ThemeListActivity" android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>